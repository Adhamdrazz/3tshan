# تقرير فحص مشروع «عطشان»

**المستودع:** [Adhamdrazz/3tshan](https://github.com/Adhamdrazz/3tshan)  
**النسخة المنشورة:** [3tshan.vercel.app](https://3tshan.vercel.app)  
**تاريخ الفحص:** 16 أغسطس 2026  

## الملخص التنفيذي

المشروع عبارة عن تطبيق ويب عربي باتجاه RTL يساعد المستخدم على العثور على أقرب مصدر مياه، واستعراض المصادر على خريطة، وإضافة مصدر جديد من خلال تحديد موقعه. البنية الحالية بسيطة ومناسبة لنموذج أولي: صفحة HTML واحدة، ملف JavaScript رئيسي، ملف CSS، ودالة Serverless واحدة للتعامل مع مصادر المياه في قاعدة Neon.

المشكلة الأهم والمثبتة عمليًا هي أن نقطة النهاية `GET /api/water-sources` المنشورة تعيد خطأ Vercel بحالة **500 – FUNCTION_INVOCATION_FAILED**. نتيجة لذلك لا تصل بيانات المصادر إلى الواجهة، وتظهر الخريطة بدون العلامات القادمة من قاعدة البيانات. ملفات JavaScript وواجهة API سليمة نحويًا في الفحص المحلي، ولذلك يجب أن يبدأ الإصلاح من إعدادات النشر وقاعدة البيانات وسجلات Vercel، وليس من إعادة كتابة الواجهة مباشرة.

## هيكل المشروع

| المسار | الدور الحالي | الملاحظات |
|---|---|---|
| `index.html` | الهيكل الكامل للتطبيق | صفحة واحدة RTL تحتوي على Views للخريطة والإضافة والحساب. |
| `script.js` | منطق التطبيق | Leaflet، تحديد الموقع، تحميل البيانات، البحث، الفلاتر، أقرب مصدر، ونموذج الإضافة. |
| `styles.css` | التصميم والاستجابة | Design tokens، تخطيط الخريطة، البطاقات، التنقل، ونقاط الاستجابة للشاشات المختلفة. |
| `api/water-sources.js` | Serverless API | يدعم `GET` و`POST` و`OPTIONS` باستخدام Neon وVercel-style handler. |
| `package.json` | إعداد Node | يحتوي على `@neondatabase/serverless` فقط، ولا يحتوي على scripts للتشغيل أو الاختبار. |
| `images/` | أصول بصرية | يحتوي على صور العلامة والشعار، مع وجود `.gitkeep`. |

## التقنيات والتدفق التشغيلي

يستخدم التطبيق **JavaScript خامًا** دون إطار مثل React، وLeaflet 1.9.4 عبر CDN، وخط Tajawal عبر Google Fonts. تُحمّل `script.js` بعد Leaflet في نهاية HTML، وهذا الترتيب صحيح. يتم إنشاء خريطة رئيسية مع طبقتي Google Streets وGoogle Satellite، ثم تُرسم علامات مصادر المياه داخل Layer Group.

عند بدء الصفحة، يستدعي الكود `getUserLocation()` و`loadWaterSources()` بالتوازي. بعد نجاح تحديد الموقع يتم وضع علامة المستخدم وتحديث مركز الخريطة، وبعد نجاح API تُخزّن المصادر في الذاكرة وتُعاد رسم العلامات. البحث والفلاتر يعملان على البيانات المحملة محليًا، بينما زر «أقرب مصدر» يحسب المسافة باستخدام معادلة Haversine.

نموذج إضافة المصدر يتيح اختيار النوع وحالة الحرارة والسعر وتحديد الإحداثيات بالنقر على خريطة فرعية. بعد الإرسال، يرسل المتصفح طلب `POST /api/water-sources`، ويُدرج الخادم السجل بحالة `pending`.

## النتائج المؤكدة

### 1. نقطة API المنشورة متوقفة

تم الوصول إلى [نقطة API المنشورة](https://3tshan.vercel.app/api/water-sources)، وكانت النتيجة صفحة Vercel تفيد بأن Serverless Function crashed مع `500: INTERNAL_SERVER_ERROR` و`FUNCTION_INVOCATION_FAILED` [3]. هذا يفسر مباشرة عدم ظهور مصادر المياه في التطبيق المنشور.

ملف API يستورد `neon` بصورة صحيحة ويستعمل `process.env.DATABASE_URL`، لذلك ينبغي فحص المتغير في Vercel، اتصال قاعدة Neon، وجود جدول `water_sources`، وتطابق أسماء الأعمدة مع الاستعلام. لا يمكن الجزم من الملفات وحدها بأي واحد من هذه الأسباب هو السبب النهائي دون سجلات Vercel أو وصول إلى إعدادات المشروع.

### 2. النموذج لا يرفع الصور فعليًا

الواجهة تحتوي على input للصورة وتعرض اسم الملف المختار، لكن معالج الإرسال لا يقرأ الملف ولا يرفعه؛ بل يرسل دائمًا `photo_url: null`. لذلك خاصية الصور شكلية في الوضع الحالي، حتى لو نجح إدخال المصدر.

### 3. الحساب والإحصائيات بيانات ثابتة

قسم «حسابي» يعرض أرقامًا ونصوصًا ثابتة داخل HTML، مثل عدد المصادر والنقاط والبلاغات. لا توجد مصادقة أو جدول مستخدمين أو ربط ديناميكي بين المساهمات والحساب. هذه ليست مشكلة عرض فحسب؛ بل تعني أن أي تطوير لنظام نقاط أو مراجعة مساهمات يحتاج إلى تصميم Backend جديد نسبيًا.

### 4. التحقق من المدخلات يحتاج تقوية

التحقق الخلفي يتأكد من وجود `type` و`temp_status` و`price_type` والإحداثيات، ويتأكد من أن الإحداثيات أرقام. لكنه لا يفرض أن latitude بين -90 و90 أو longitude بين -180 و180، ولا يفرض مجموعة قيم مسموحة للنوع والحرارة والسعر، ولا يضع حدودًا لطول `name` أو `photo_url`.

### 5. اعتبارات الأمان والاعتمادية

إعداد CORS في API يسمح لأي Origin، وهو مقبول أحيانًا لنموذج عام، لكنه يوسّع سطح الاستخدام غير المرغوب فيه. كما أن استجابة الخطأ تعيد `error.message` إلى العميل، وقد تكشف تفاصيل داخلية من قاعدة البيانات أو بيئة التشغيل. لا يوجد Rate Limiting أو CAPTCHA أو آلية تمنع الإرسال الآلي المتكرر، مع أن كل POST ينشئ سجلًا جديدًا بحالة `pending`.

### 6. حالة البيانات المعروضة

استعلام GET يعيد كل السجلات مرتبة حسب `created_at`، بما في ذلك السجلات ذات الحالة `pending`، لأن الاستعلام لا يحتوي على شرط `WHERE status = 'approved'`. إذا كانت الحالة `pending` تعني انتظار المراجعة، فهذا قد يسمح بعرض مصادر غير مراجعة للمستخدمين. يجب تأكيد سياسة الحالة المطلوبة قبل تعديل ذلك.

### 7. قابلية الصيانة

الملف `script.js` يبلغ نحو 2100 سطر، و`index.html` نحو 1500 سطر. الكود منظم بتعليقات تقسّم الأقسام، لكنه ما زال شديد المركزية ويحتوي على HTML طويل داخل JavaScript وInline styles داخل HTML. هذا سيجعل إضافة المصادقة، المراجعة، الإشعارات، أو إدارة الصور أكثر صعوبة مع الوقت.

## نقاط جيدة في التنفيذ الحالي

يوجد استخدام جيد لـ `escapeHtml` قبل إدراج اسم المصدر داخل popup، وهذا يقلل خطر XSS في هذا الموضع تحديدًا. كما أن حساب المسافات والتحقق من صلاحية الإحداثيات قبل إنشاء العلامات موجودان في الواجهة، ويُعطّل زر الإرسال أثناء الطلب لتقليل النقرات المكررة. كذلك توجد معالجة لرفض تحديد الموقع مع الرجوع إلى إحداثيات افتراضية في القاهرة.

## ترتيب الأولويات المقترح

| الأولوية | الإجراء | الهدف |
|---|---|---|
| P0 | إصلاح `GET /api/water-sources` من إعدادات Vercel/Neon | إعادة البيانات إلى التطبيق قبل أي تطوير جديد. |
| P0 | إخفاء تفاصيل أخطاء قاعدة البيانات من الاستجابة العامة | تحسين الأمان وإبقاء التفاصيل في السجلات فقط. |
| P1 | إضافة تحقق صارم للقيم والنطاقات والأطوال | منع البيانات غير الصحيحة والإدخالات الضارة. |
| P1 | تحديد سياسة عرض `pending` و`approved` | منع ظهور مصادر غير مراجعة إن كان ذلك غير مقصود. |
| P1 | فصل إعدادات API وطبقة البيانات عن handler | تحسين الاختبار وقابلية التوسع. |
| P2 | تنفيذ رفع الصور إلى Storage وربط `photo_url` | جعل خاصية الصور حقيقية بدلًا من كونها واجهة فقط. |
| P2 | إضافة مصادقة وجداول المستخدمين والمساهمات | تحويل صفحة الحساب والنقاط إلى بيانات فعلية. |
| P2 | إضافة اختبارات API وفحص تلقائي قبل النشر | منع عودة خطأ 500 ومشكلات التحقق. |
| P3 | تقسيم JavaScript وHTML إلى وحدات أو الانتقال إلى بنية مكونات | خفض تكلفة التطوير المستقبلي. |

## الخلاصة

المشروع مناسب كأساس أولي لتطبيق خرائط مجتمعي، والواجهة الأساسية مكتملة بصريًا إلى حد جيد. لكن قبل البدء في أي ميزة كبيرة يجب إصلاح API المنشورة والتحقق من قاعدة البيانات؛ فهذه هي نقطة الاختناق الحالية. بعد ذلك أنصح بتنفيذ دورة قصيرة من ثلاث خطوات: تثبيت عقد البيانات والسياسات، تقوية API والتحقق، ثم بناء الميزة التالية فوق طبقة مستقرة.

## المراجع

[1]: https://github.com/Adhamdrazz/3tshan "المستودع الرسمي على GitHub"
[2]: https://3tshan.vercel.app "النسخة المنشورة من التطبيق"
[3]: https://3tshan.vercel.app/api/water-sources "نقطة API المنشورة لمصادر المياه"
[4]: https://raw.githubusercontent.com/Adhamdrazz/3tshan/main/api/water-sources.js "ملف API الخام"
[5]: https://raw.githubusercontent.com/Adhamdrazz/3tshan/main/script.js "ملف JavaScript الخام"
