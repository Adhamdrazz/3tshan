# تقرير التدقيق الشامل لمشروع «عطشان»

**تاريخ التدقيق:** 16 أغسطس 2026  
**النسخة التي تم فحصها:** [3tshan-app.vercel.app](https://3tshan-app.vercel.app)  
**المستودع:** [Adhamdrazz/3tshan](https://github.com/Adhamdrazz/3tshan)

## الملخص التنفيذي

المشروع يعمل حاليًا كنسخة أولية قابلة للاستخدام: الخريطة تفتح، المصادر المعتمدة تصل من قاعدة البيانات، نموذج الإضافة ظاهر على الهاتف، وصفحة الحساب تعرض نظام اللاعب والنقاط. أهم شيء إيجابي هو أن المسار الأساسي للمستخدم موجود من البداية إلى النهاية: يفتح الخريطة، يبحث أو يفلتر، يرى المصدر، يفتح صفحة الإضافة، ويرسل المصدر للمراجعة.

لكن المشروع ما زال يحتاج إلى **مرحلة تثبيت قبل إضافة ميزات كبيرة جديدة**. أكبر المخاطر الحالية ليست في الشكل، بل في حجم الصور، الاعتماد على هوية اللاعب المحلية، غياب نظام حسابات حقيقي، قابلية تزوير الأحداث والنقاط، غياب تحديد معدل الطلبات، وضعف قابلية الصيانة بسبب تركيز معظم المنطق في ملفين كبيرين. لذلك أوصي ألا نضيف صفحات كثيرة الآن؛ الأفضل أن نثبت البيانات والأمان وتجربة الإضافة أولًا، ثم نناقش تطوير الحسابات واللعبة الاجتماعية.

## الحالة الحالية المؤكدة

| المجال | الحالة | التقييم |
|---|---|---|
| الصفحة الرئيسية والخريطة | تعمل وتعرض واجهة RTL وأزرار البحث والفلترة والأقرب | جيدة كنواة أولية |
| المصادر العامة | نقطة API المنشورة أعادت 200، والبيانات العامة الحالية كلها `approved` | يعمل، لكن يحتاج pagination وتحسين payload |
| نموذج إضافة المصدر | يفتح على الهاتف والويب، وزر الإرسال يظهر بعد الملاحظة | يعمل وظيفيًا |
| مراجعة المشرف | API محمي بـ `ADMIN_TOKEN` وتوجد تبويبات للمراجعة والمعتمد | يعمل، لكن يحتاج حماية أقوى وإدارة حقيقية |
| الحساب واللاعب | نقاط ومستويات وسجل مساهمات مرتبط بمعرف محلي | مناسب للتجربة، غير كافٍ لحساب موحد |
| الصور | تُحفظ حاليًا داخل `photo_url` كـ data URL base64 | خطر أداء وتخزين |
| الاختبارات | فحوص syntax موجودة، ولا توجد اختبارات API أو اختبارات متصفح آلية | يحتاج تأسيسًا |
| الصيانة | `script.js` حوالي 2262 سطرًا و`index.html` حوالي 1382 سطرًا | عالية التكلفة مستقبلًا |

## ما تم فحصه فعليًا

تم فحص ملفات `index.html` و`script.js` و`styles.css` و`admin.html` و`api/water-sources.js` و`schema.sql` و`player_progress_migration.sql`، مع مراجعة الحزم الحالية، حالة المستودع، والنسخة المنشورة. كما تم فتح النسخة المنشورة بصريًا، وفحص صفحة الحساب ونموذج إضافة المصدر، وطلب نقطة API العامة ونقطة API الخاصة بملف اللاعب دون معرف.

## النتائج المهمة

### 1. الصور هي أكبر مشكلة أداء حالية — أولوية P0

النسخة العامة تعيد أربعة مصادر معتمدة، لكن حجم استجابة `GET /api/water-sources` كان تقريبًا **1,010,398 بايت**. يوجد مصدران يحتويان على صور base64، وحجم الصور المضمنة يقارب **1,009,260 حرفًا**، وأكبر صورة تقارب **868,822 حرفًا**. هذا يعني أن المستخدم يحمل صور المصادر كاملة مع بيانات الخريطة حتى لو لم يفتح أي صورة.

النتيجة العملية هي بطء أول تحميل، استهلاك بيانات الهاتف، ضغط أكبر على Vercel وNeon، واحتمال أن تصبح الخريطة بطيئة جدًا عند زيادة عدد المصادر. الحل الصحيح هو رفع الصور إلى تخزين ملفات مستقل، حفظ رابط صغير فقط في قاعدة البيانات، وإرجاع الصور المصغرة أو عدم إرجاع الصورة في طلب الخريطة أصلًا.

### 2. نظام اللاعب مفيد نفسيًا لكنه ليس هوية مستخدم حقيقية — أولوية P1

النقاط مرتبطة بمعرف يولّده المتصفح ويخزنه في `localStorage`. لذلك يستمر العداد في نفس المتصفح ما لم تُحذف بياناته، لكنه لا ينتقل تلقائيًا إلى هاتف آخر أو متصفح آخر أو تطبيق Android. كما أن أي مستخدم يستطيع تقنيًا تغيير المعرف أو إرسال طلبات مباشرة، لأن النظام لا يملك حسابًا موثقًا.

هذا مناسب كـ **نسخة تجريبية للعبة**، لكنه غير مناسب إذا أردنا ترتيبًا تنافسيًا أو شارات موثوقة أو حماية من التحايل. التطوير المنطقي لاحقًا هو حساب اختياري برقم الهاتف أو Google، مع الاحتفاظ بخيار التجربة دون تسجيل دخول.

### 3. احتساب النقاط يحتاج إلى مصدر حقيقة واحد — أولوية P1

حاليًا يحصل اللاعب على 10 نقاط عند إرسال المصدر، ثم 50 نقطة عند اعتماد المشرف. هذا تصميم مفهوم ويشجع الإضافة، لكن قيمة النقاط تُكتب في أكثر من مكان: داخل `player_profiles` وداخل `water_sources.points_awarded`. لا توجد حاليًا حركة نقاط مستقلة تسجل سبب كل زيادة.

الأفضل إنشاء جدول `point_transactions` يسجل: اللاعب، عدد النقاط، السبب، المصدر، وتاريخ العملية. عندها نستطيع معرفة لماذا حصل المستخدم على كل نقطة، ومنع التكرار بطريقة موثوقة، وبناء سجل إنجازات ولوحة ترتيب مستقبلًا.

### 4. الأحداث والإحصائيات قابلة للتلاعب — أولوية P1

نقطة `POST /api/water-sources?event=1` عامة وتقبل `visit` و`source_view` و`nearest_click` وغيرها دون توثيق قوي أو تحديد معدل الطلبات. أي عميل يستطيع إرسال آلاف الأحداث يدويًا. لذلك أرقام الزيارات والتفاعلات في لوحة الإدارة مفيدة كمؤشر تقريبي فقط، وليست Analytics موثوقة.

ينبغي إضافة rate limiting، منع تكرار الزيارة من نفس الجلسة خلال مدة قصيرة، وتحديد الأحداث المسموح بها حسب مصدرها. كما ينبغي ألا يحصل أي نظام نقاط على بيانات من أحداث يرسلها العميل مباشرة دون تحقق الخادم.

### 5. لا يوجد rate limiting أو حماية من الإرسال الآلي — أولوية P1

نقطة إضافة المصدر عامة، وكل طلب صالح ينشئ سجلًا جديدًا بحالة `pending`. لا يوجد حد يومي أو حد زمني لكل عنوان/جلسة، ولا CAPTCHA أو آلية تحد من الرسائل الآلية. هذا يسمح بإغراق لوحة الإدارة بمصادر وهمية، أو استهلاك قاعدة البيانات، أو استهلاك خدمة Nominatim عبر reverse geocoding.

نقترح حدًا أوليًا بسيطًا: عدد محدود من الإضافات لكل معرف جلسة خلال الساعة، ثم CAPTCHA عند تجاوز الحد، مع وضع geocoding في مسار مؤجل أو cache بدل استدعائه داخل طلب المستخدم مباشرة.

### 6. طلب الإضافة ينتظر Nominatim داخل نفس الطلب — أولوية P1

عند إضافة المصدر، يستدعي الخادم Nominatim ثم يكتب إلى Neon. هذا يجعل زمن الاستجابة معتمدًا على خدمة خارجية، وقد يتعطل الإرسال أو يتأخر إذا كانت الخدمة بطيئة. كما أن كثرة الطلبات قد تخالف سياسة الاستخدام المناسبة لخدمة عامة.

الأفضل حفظ المصدر أولًا بإحداثياته، ثم تنفيذ reverse geocoding في مهمة مؤجلة أو endpoint إداري محدود، مع cache للإحداثيات القريبة. إذا فشل geocoding يجب ألا يفشل إنشاء المصدر نفسه.

### 7. لا يوجد pagination في المصادر العامة أو لوحة الإدارة — أولوية P1

الطلب العام يعيد كل المصادر المعتمدة، وطلب المشرف `status=all` يعيد كل السجلات. هذا مقبول مع أربعة مصادر، لكنه سيتحول إلى مشكلة عند مئات أو آلاف السجلات، خصوصًا مع الصور base64. المطلوب إضافة `limit` و`cursor` أو `page`، وفصل قائمة الخريطة عن تفاصيل المصدر والصورة.

### 8. الإدارة محمية بتوكن مشترك وليس حسابات مشرفين — أولوية P1

الحماية الحالية تعتمد على `ADMIN_TOKEN` المشترك في header أو Bearer token، وهذا أفضل من ترك الإدارة عامة، لكنه يعني أن كل من يعرف التوكن يملك صلاحيات الاعتماد والحذف والإحصائيات. التوكن محفوظ في `sessionStorage` داخل المتصفح، ولا توجد أدوار أو سجل تدقيق لمن نفذ عملية الحذف أو الاعتماد.

قبل إطلاق واسع، نحتاج حساب مشرف حقيقي أو على الأقل توكنات منفصلة قابلة للإلغاء، وسجل `admin_audit_log` يسجل العملية والوقت والمعرف. كما يجب التفكير في حماية صفحة `admin.html` نفسها من الفهرسة وإضافة `robots.txt` مناسب.

### 9. لوحة الإدارة تحتوي على أجزاء شكلية غير متصلة ببيانات حقيقية — أولوية P2

أقسام المستخدمين والتقييمات والبلاغات تعرض رسائل تفيد بأنها غير مفعلة. هذا أفضل من عرض بيانات وهمية، لكنه قد يوحي للمشرف أن النظام مكتمل. أقترح إبقاء هذه العناصر مخفية أو وضع شارة «قريبًا» واضحة، ثم بناء البلاغات قبل التقييمات لأن البلاغات أهم لسلامة بيانات المياه.

### 10. قابلية الصيانة ضعيفة بسبب الملفات المركزية — أولوية P2

معظم الواجهة في `index.html` و`script.js`، مع HTML طويل يتم توليده داخل JavaScript ووجود inline styles كثيرة. هذا يجعل إصلاح الهاتف أو إضافة ميزة يتطلب البحث في أماكن كثيرة، ويزيد احتمال أن يؤثر تعديل بسيط في الخريطة على الحساب أو النموذج.

لا أقترح إعادة كتابة المشروع كاملًا الآن. الأفضل تقسيمه تدريجيًا إلى وحدات صغيرة: `map.js`، `sources.js`، `add-source.js`، `profile.js`، `api-client.js`، وملف CSS للأجزاء المتخصصة. يمكن تنفيذ ذلك على مراحل دون تغيير التقنية الحالية.

### 11. لا توجد اختبارات تشغيل حقيقية — أولوية P1

الفحص النحوي يثبت أن JavaScript قابل للقراءة فقط، ولا يثبت أن API تتصل بقاعدة Neon أو أن زر الاعتماد يضيف النقاط أو أن الهاتف يعرض زر الإرسال بعد التمرير. لا توجد اختبارات API، ولا اختبارات للتأكد من أن المصدر `pending` لا يظهر للعامة، ولا اختبار يمنع مضاعفة نقاط الاعتماد.

الحد الأدنى المقترح هو اختبارات endpoint للآتي: العرض العام يعيد approved فقط، الطلب غير المصرح به لا يعيد pending/all، الاعتماد يضيف نقاطًا مرة واحدة، والإحداثيات والقيم غير الصحيحة تُرفض.

## ملاحظات تجربة المستخدم

النسخة المنشورة تعطي مسارًا واضحًا من الخريطة إلى الإضافة والحساب، وهذا جيد. نموذج الإضافة ظاهر على الهاتف، وزر الإرسال يصل إليه المستخدم بعد النزول أسفل الصورة والملاحظة. لكن الحقول كثيرة نسبيًا، والصورة إلزامية، وخريطة تحديد الموقع تحتاج شرحًا أقوى للمستخدم الذي لا يعرف أنه يجب أن ينقر عليها.

صفحة الحساب أصبحت تحمل فكرة اللاعب، وهذا اتجاه صحيح. مع ذلك، توجد حاليًا طبقة اللاعب، ثم إحصائيات مساهمات، ثم قسم عن عطشان؛ في الهاتف قد تبدو الصفحة طويلة ومكررة. قبل إضافة شارات ولوحة ترتيب، يجب تحديد ثلاث لحظات رئيسية فقط: «نقاطي»، «ما الذي أضفته»، و«ماذا أفعل الآن لأتقدم؟».

## ما لا أنصح بإضافته الآن

لا أنصح الآن بإضافة عشرات عناصر القائمة، أو نظام تقييم، أو دردشة، أو صفحات دول مستقلة، أو تحويل المشروع إلى Android قبل تثبيت الصور والحسابات والاختبارات. هذه الميزات ستزيد الحجم والتعقيد بينما الأساس ما زال يعتمد على `localStorage` وصور base64 وطلبات بلا rate limit.

## خارطة التطوير المقترحة

| المرحلة | ما سنبنيه | سبب الترتيب |
|---|---|---|
| 0 — تثبيت | تخزين الصور، pagination، rate limit، logging، اختبارات API، إزالة الملفات المضغوطة من المستودع | يمنع مشاكل الإنتاج قبل النمو |
| 1 — اللاعب الحقيقي | حساب اختياري، جدول معاملات النقاط، استرجاع النقاط عبر الأجهزة | يحول اللاعب من هوية متصفح إلى مستخدم حقيقي |
| 2 — الثقة والسلامة | بلاغ عن مصدر مغلق/غير صحيح، حالة المصدر، سجل مراجعة المشرف | يجعل الخريطة يعتمد عليها الناس |
| 3 — التحفيز | شارات، مستويات، أهداف يومية أو أسبوعية، رسائل أثر حقيقية | يزيد المشاركة بعد ضمان صحة البيانات |
| 4 — التطبيق | PWA ثم Android wrapper أو تطبيق أصلي عند ثبات التجربة | يقلل خطر تحويل مشاكل الويب إلى تطبيق |

## القرارات التي نناقشها قبل التنفيذ

أحتاج رأيك في ثلاثة قرارات فقط قبل أن أبدأ أي تعديل جديد. أولًا: هل تريد أن تكون النقاط مرتبطة بتسجيل دخول حقيقي، أم نُبقي التجربة دون تسجيل لفترة مؤقتة؟ ثانيًا: هل اعتماد المصدر يمنح 50 نقطة كما هو الآن، أم تريد قيمة مختلفة؟ ثالثًا: هل نرفع الصور إلى تخزين ملفات حقيقي ونحفظ الرابط فقط، وهو الحل الذي أوصي به بشدة؟

رأيي المهني هو أن نبدأ بالمرحلة صفر، وبالتحديد تخزين الصور واختبارات API ومنع التكرار، ثم ننتقل إلى الحساب الحقيقي. بعد موافقتك على هذه القرارات لن أضيف أي شاشة أو ميزة عشوائية؛ سنبني كل خطوة على سبب واضح ونختبرها قبل الانتقال إلى التالية.

## المراجع

[1]: https://github.com/Adhamdrazz/3tshan "المستودع الرسمي لمشروع عطشان"
[2]: https://3tshan-app.vercel.app "النسخة المنشورة من تطبيق عطشان"
[3]: https://3tshan-app.vercel.app/api/water-sources "نقطة API العامة للمصادر"
[4]: https://nominatim.openstreetmap.org/ "خدمة Nominatim المستخدمة في تحديد الدولة والمحافظة"
