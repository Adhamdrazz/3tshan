# تعليمات نشر إصلاحات عطشان

## 1. قاعدة البيانات

افتح مشروع Neon وشغّل محتوى الملف `schema.sql` مرة واحدة. إذا كان جدول `water_sources` موجودًا مسبقًا، راجع القيود والفهارس يدويًا قبل تنفيذ أي تغييرات بنيوية.

## 2. ترحيل الدولة والمحافظة

شغّل ملف `countries_migration.sql` في Neon مرة واحدة. هذا يضيف حقلي `country` و`province` ويملأ المصادر الحالية المعروفة حسب إحداثياتها. المصادر الجديدة ستُملأ تلقائيًا عند الإضافة.

## 3. متغيرات Vercel

أضف المتغير التالي في بيئات Preview وProduction:

```text
DATABASE_URL=رابط اتصال Neon
ADMIN_TOKEN=رمز سري طويل وعشوائي للمشرف
```

اجعل `ADMIN_TOKEN` قيمة طويلة لا تُستخدم في أي مكان آخر، ولا تضعها داخل GitHub أو ملفات HTML.

اختياريًا، إذا كانت هناك حاجة لاستخدام API من نطاق آخر، أضف:

```text
ALLOWED_ORIGIN=https://3tshan.vercel.app
```

لا تضع هذه القيم داخل الملفات أو المستودع العام.

## 4. النشر

ارفع الملفات المعدلة إلى فرع `main` أو نفّذ Deploy من Vercel. بعد انتهاء النشر اختبر:

```text
GET https://3tshan.vercel.app/api/water-sources
```

يجب أن تعود استجابة JSON من الشكل:

```json
{"success":true,"data":[]}
```

## 5. تسجيل دخول المشرف واعتماد المصادر وDark Mode

تسجيل دخول المشرف يتحقق من `ADMIN_TOKEN` عبر API قبل فتح اللوحة، لذلك أي كلمة خاطئة سيتم رفضها ولن تظهر البيانات.

المصدر الجديد يُحفظ تلقائيًا بحالة `pending`، ولا يعيده الطلب العام للمستخدمين. المشرف يفتح `admin.html`، ثم يضغط قبول أو رفض. عند القبول تتحول الحالة إلى `approved` ويظهر المصدر في الخريطة العامة، أما الرفض فيبقى خارج الخريطة.

زر القمر في أسفل القائمة يفعّل الوضع الليلي ويحفظ التفضيل على نفس المتصفح.

## 6. لوحة التحليل والتصدير والزيارات

شغّل تحديث `schema.sql` مرة واحدة لإنشاء جدول `site_events`. يسجل التطبيق الزيارة والتفاعلات العامة دون تخزين اسم المستخدم أو بريده أو عنوان IP. تظهر الزيارات والتفاعلات داخل بطاقات لوحة التحكم بعد وجود بيانات فعلية.

قسم المصادر في لوحة الإدارة منفصل إلى تبويبين: **قيد المراجعة** للقبول والرفض فقط، و**المصادر المعتمدة** للتعديل أو الحذف لاحقًا.

## 7. لوحة التحليل والتصدير

بعد النشر افتح `admin.html`. ستظهر بطاقات حقيقية من قاعدة البيانات، تشمل إجمالي المصادر، قيد المراجعة، المعتمد، والمجاني، بالإضافة إلى أشرطة للكولديرات حسب الدولة والمحافظة وتوزيع الأنواع والحالات. زر `تصدير Excel` ينزّل ملف CSV يمكن فتحه مباشرة في Excel.

## 8. اختبار الإضافة

بعد نجاح GET، افتح لوحة المراجعة من:

```text
https://3tshan.vercel.app/admin.html
```

أدخل قيمة `ADMIN_TOKEN`، ثم اختبر قبول أو رفض مصدر من القائمة. بعد ذلك اختبر نموذج «أضف مصدرًا» من الواجهة. السجل الجديد سيُحفظ بحالة `pending`. إذا كانت سياسة التطبيق تتطلب اعتماد المصادر قبل عرضها، فأنشئ لاحقًا واجهة إدارية لتغيير الحالة إلى `approved`.

## 9. الصور

يستخدم استخراج الدولة والمحافظة خدمة Nominatim عند إضافة المصدر فقط، ثم يخزن النتيجة في قاعدة البيانات. يجب عدم تحويله إلى طلبات متكررة عند فتح الصفحة، والالتزام بحدود الخدمة ونسب بيانات OpenStreetMap في واجهة التطبيق.

الإصلاح الحالي يقرأ الصور في المتصفح ويرسلها كـ Data URL بحد أقصى 750 كيلوبايت. هذا حل مناسب للنموذج الأولي، لكنه ليس الحل المثالي للإنتاج؛ عند زيادة الاستخدام يجب نقل الصور إلى Object Storage وحفظ الرابط فقط في `photo_url`.

## 10. فحص محلي

```bash
npm install
npm run check
```

الأمر `npm run check` يتحقق من صياغة ملفي JavaScript الأساسي وواجهة API.
