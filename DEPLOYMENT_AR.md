# تعليمات نشر إصلاحات عطشان

## 1. قاعدة البيانات

افتح مشروع Neon وشغّل محتوى الملف `schema.sql` مرة واحدة. إذا كان جدول `water_sources` موجودًا مسبقًا، راجع القيود والفهارس يدويًا قبل تنفيذ أي تغييرات بنيوية.

## 2. متغيرات Vercel

أضف المتغير التالي في بيئات Preview وProduction:

```text
DATABASE_URL=رابط اتصال Neon
ADMIN_TOKEN=رمز سري طويل وعشوائي للمشرف
```

اجعل `ADMIN_TOKEN` قيمة طويلة لا تُستخدم في أي مكان آخر، ولا تضعها داخل GitHub أو ملفات HTML.

اختياريًا، إذا كانت هناك حاجة لاستخدام API من نطاق آخر، أضف:

```text
ALLOWED_ORIGIN=https://3tshan.vercel.app
```

لا تضع هذه القيم داخل الملفات أو المستودع العام.

## 3. النشر

ارفع الملفات المعدلة إلى فرع `main` أو نفّذ Deploy من Vercel. بعد انتهاء النشر اختبر:

```text
GET https://3tshan.vercel.app/api/water-sources
```

يجب أن تعود استجابة JSON من الشكل:

```json
{"success":true,"data":[]}
```

## 4. اختبار الإضافة

بعد نجاح GET، افتح لوحة المراجعة من:

```text
https://3tshan.vercel.app/admin.html
```

أدخل قيمة `ADMIN_TOKEN`، ثم اختبر قبول أو رفض مصدر من القائمة. بعد ذلك اختبر نموذج «أضف مصدرًا» من الواجهة. السجل الجديد سيُحفظ بحالة `pending`. إذا كانت سياسة التطبيق تتطلب اعتماد المصادر قبل عرضها، فأنشئ لاحقًا واجهة إدارية لتغيير الحالة إلى `approved`.

## 5. الصور

الإصلاح الحالي يقرأ الصور في المتصفح ويرسلها كـ Data URL بحد أقصى 750 كيلوبايت. هذا حل مناسب للنموذج الأولي، لكنه ليس الحل المثالي للإنتاج؛ عند زيادة الاستخدام يجب نقل الصور إلى Object Storage وحفظ الرابط فقط في `photo_url`.

## 6. فحص محلي

```bash
npm install
npm run check
```

الأمر `npm run check` يتحقق من صياغة ملفي JavaScript الأساسي وواجهة API.
